services:
  db:
    container_name: foodgram-db
    image: postgres:13
    environment:
      POSTGRES_DB: foodgram_db
      POSTGRES_USER: django_user
      POSTGRES_PASSWORD: Kuzia_26148
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U django_user -d foodgram_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  backend:
    container_name: foodgram-backend
    build:
      context: ..
      dockerfile: backend/Dockerfile
    expose:
      - "8005"
    volumes:
      - ./staticfiles:/app/staticfiles
      - ./media:/app/media
    environment:
      - POSTGRES_DB=foodgram_db
      - POSTGRES_USER=django_user
      - POSTGRES_PASSWORD=Kuzia_26148
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=django-insecure-yndk(x4#&3mf06--)(k^^v%8^$9_kq&=##6eumoha+=hzjae^b
      - DEBUG=False
      - ALLOWED_HOSTS=foodgramkuzmich.zapto.org,89.169.175.209,localhost,127.0.0.1
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    container_name: foodgram-nginx
    image: nginx:1.25
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./staticfiles:/app/staticfiles
      - ./media:/app/media
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
